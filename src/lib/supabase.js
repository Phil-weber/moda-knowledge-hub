// Re-export the shared Supabase client (configured for Lovable Cloud).
// Use this from anywhere in the app: `import { supabase } from "@/lib/supabase"`.
export { supabase } from "@/integrations/supabase/client";
